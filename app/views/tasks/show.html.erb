<h2><%= @tasks.title %></h2>
<p><%= @tasks.details %></p>

<%= link_to("Other tasks", tasks_path) %>
<%= link_to("Edit this task", edit_path(@tasks)) %>
<%= link_to("Delete this task", destroy_path(@tasks), method: :delete) %>
